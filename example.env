# Django Configuration
DJANGO_SECRET_KEY=your-secret-key-here

# Database Configuration
# For local PostgreSQL (when USE_POSTGRES=1 in docker-compose)
POSTGRES_DB=credit_approval
POSTGRES_USER=user
POSTGRES_PASSWORD=password

# For external PostgreSQL (like Neon, Supabase, etc.)
# Replace with your actual database URL
DATABASE_URL=postgresql://username:password@host:port/database?sslmode=require

# Example Neon URL format:
# DATABASE_URL=postgresql://neondb_owner:password@ep-cool-lab-a5cy1pl7-pooler.us-east-2.aws.neon.tech/neondb?sslmode=require&channel_binding=require

# Docker User Configuration (to match host user permissions)
# Set these to match your host user ID and group ID to avoid permission issues
# USER_ID=1000
# GROUP_ID=1000

# Redis Configuration (for Celery)
# Usually configured in docker-compose.yml, but can be overridden here
# CELERY_BROKER_URL=redis://redis:6379/0
# CELERY_RESULT_BACKEND=redis://redis:6379/0

# Django Superuser (for docker-compose setup)
DJANGO_SUPERUSER_USERNAME=admin
DJANGO_SUPERUSER_PASSWORD=admin123
DJANGO_SUPERUSER_EMAIL=admin@example.com